name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  DOCKER_USERNAME: alfa2003
  BACKEND_IMAGE: alfa2003/crud-backend
  FRONTEND_IMAGE: alfa2003/crud-frontend

jobs:
  # Job 1: Build et Test Backend
  backend:
    name: Backend CI/CD
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🏗️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./backend
      run: npm ci
    
    - name: 🧪 Run tests
      working-directory: ./backend
      run: npm test || echo "No tests configured"
    
    - name: 🔐 Login to Docker Hub
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: 🏷️ Extract metadata
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.BACKEND_IMAGE }}
        tags: |
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}
          type=semver,pattern={{version}}
    
    - name: 🐳 Build and push Docker image
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  # Job 2: Build et Test Frontend
  frontend:
    name: Frontend CI/CD
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🏗️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./frontend
      run: npm ci
    
    - name: 🏗️ Build application
      working-directory: ./frontend
      run: npm run build
    
    - name: 🧪 Run tests
      working-directory: ./frontend
      run: npm test || echo "No tests configured"
    
    - name: 🔐 Login to Docker Hub
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: 🏷️ Extract metadata
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.FRONTEND_IMAGE }}
        tags: |
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}
          type=semver,pattern={{version}}
    
    - name: 🐳 Build and push Docker image
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        file: ./frontend/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  # Job 3: Deploy (optionnel - décommenter si vous voulez le déploiement auto)
  # deploy:
  #   name: Deploy to Kubernetes
  #   runs-on: ubuntu-latest
  #   needs: [backend, frontend]
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - name: 📥 Checkout code
  #     uses: actions/checkout@v4
  #   
  #   - name: 🔧 Setup kubectl
  #     uses: azure/setup-kubectl@v3
  #     with:
  #       version: 'latest'
  #   
  #   - name: 🔐 Configure kubectl
  #     run: |
  #       mkdir -p $HOME/.kube
  #       echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > $HOME/.kube/config
  #   
  #   - name: 🚀 Deploy to Kubernetes
  #     run: |
  #       kubectl apply -f k8s/
  #       kubectl rollout restart deployment/backend-deployment
  #       kubectl rollout restart deployment/frontend-deployment
  #   
  #   - name: ⏳ Wait for rollout
  #     run: |
  #       kubectl rollout status deployment/backend-deployment --timeout=5m
  #       kubectl rollout status deployment/frontend-deployment --timeout=5m
  #   
  #   - name: ✅ Verify deployment
  #     run: |
  #       kubectl get pods
  #       kubectl get services